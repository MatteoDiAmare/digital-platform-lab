# Lab 02: HTML

Inspect and change the structure of a simple web page.

Students work with common HTML elements, attributes, forms, buttons, and data attributes. The aim is to connect visible interface elements to the document structure used by the browser and by JavaScript.
