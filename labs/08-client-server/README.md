# Lab 08: Client and server

Send an event from the browser to a server endpoint and inspect the response.

Students follow the request payload, HTTP method, status code, response body, and server log. The lab shows what each acknowledgement confirms and what it does not confirm.
