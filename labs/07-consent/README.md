# Lab 07: Consent

Make data collection respond to a user's consent choice.

Students compare necessary application behaviour with optional tracking. They modify the client so that a consent state affects which events are created or sent, then verify the result in the browser.
