# Lab 03: JavaScript events

Connect a user action to client-side behaviour.

Students inspect and modify small JavaScript functions and event listeners. The lab shows how a click or form submission becomes a structured event that can be logged, validated, or sent elsewhere.
