# Lab 05: Identification

Examine how a digital service recognises a browser, session, or signed-in user.

Students work with synthetic identifiers and test what happens when storage is cleared, a new session is started, or the same person uses another browser. The lab separates technical identifiers from claims about a real person.
