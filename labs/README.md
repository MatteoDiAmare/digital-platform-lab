# Labs

This directory contains the practical exercises used during the course.

Each lab focuses on one part of the same small web application. Labs must be self-contained, use synthetic data, and include clear verification steps. A student should be able to tell whether the expected behaviour occurred by using the page, the browser developer tools, or the server log.

The numbered directories follow the planned teaching sequence. Their content may be adjusted when the lesson plans are finalised.
