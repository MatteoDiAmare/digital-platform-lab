# Public application files

This directory will contain the files loaded by the browser:

- `index.html` for page structure
- `styles.css` for presentation
- `app.js` for client-side behaviour

The files are kept separate so students can see which part controls structure, presentation, and behaviour. Later exercises will use these files to inspect DOM events, browser storage, tracking events, consent choices, and network requests.
